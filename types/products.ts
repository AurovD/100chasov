export interface ProductsType {
    id: string;
    title: string;
    description: string;
    price: string;
    count: number;
    link: string;
    category_id: string;
    images: string[];
    message?: string;
}
